query FetchCountries($filter: CountryFilterInput){
  countries(filter: $filter){
    name,
    code,
    emoji,
    phone,
    continent {
      name
    },
    capital,
    currency,
    states {
      name,
    }
  }
}